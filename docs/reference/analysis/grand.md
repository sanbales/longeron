# Grand tour dashboard

Requires the `viz` extra for the widgets and the `mdao`/`smt` extras for
the sizing and verdict strips (`pip install "longeron[viz,mdao,smt]"`).

```{eval-rst}
.. automodule:: longeron.analysis.grand
```
