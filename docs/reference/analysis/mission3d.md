# Mission 3D tracks

Requires the `viz` extra (`pip install "longeron[viz]"`).

The Cesium viewer widget that plays these tracks lives in
{doc}`longeron.widgets.mission3d </reference/widgets/mission3d>`; this
module is the kernel-side track/CZML synthesis.

```{eval-rst}
.. automodule:: longeron.analysis.mission3d
```
