# Replay

Requires the `replay` extra (`pip install "longeron[replay]"`).

The replay widget itself lives in
{doc}`longeron.widgets.replay </reference/widgets/replay>`; this module
is the kernel-side timeline recorder.

```{eval-rst}
.. automodule:: longeron.replay
```
