# Mission viewer

Requires the `viz` extra (`pip install "longeron[viz]"`).

The track/CZML synthesis this widget plays lives in
{doc}`longeron.analysis.mission3d </reference/analysis/mission3d>`.

```{eval-rst}
.. automodule:: longeron.widgets.mission3d
```
