# Replay widget

Requires the `replay` extra (`pip install "longeron[replay]"`).

The timeline recorders this widget animates live in
{doc}`longeron.replay </reference/replay>`.

```{eval-rst}
.. automodule:: longeron.widgets.replay
```
